NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Server-only. Needed now for /api routes (order creation, payment
# verification, webhook) — never exposed to the browser.
SUPABASE_SERVICE_ROLE_KEY=""

# Razorpay — all server-only except the key id, which the create-order API
# route hands back to the client per-checkout (never stored as
# NEXT_PUBLIC_*, since the client should only ever see it at the moment
# it's actually opening a payment).
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""
